# 세션 플로우 스펙 (Session Flow Specification)

> **버전:** v1.7 (2026-09-04 — 컨테이너 협의 확정 반영)
> **역할:** 문제풀이 세션 기획의 **단일 진실 원천(Single Source of Truth)**. 화면 흐름·상태·채점·리포트의 기획 확정본.
> **관련 문서:** API 계약(BE↔AI컨테이너) = `03_AI_Container_Contract.md` · 클라↔BE API = `05_API_Design.md` · 스키마 = `04_Database_Design.md`

---

## 1. 용어 정의 (팀 공식)

| 용어 | 정의 |
|------|------|
| **AI 컨테이너** | 통합 AI 컨테이너 — 문제 출제 + 채점 + 리포트 담당 (구 llm-container + scoring-container 통합) |
| **발화지표** | `VOICE_RECORD`의 `SYLLABLES` / `SPEAKING_TIME` / `ARTICULATION_TIME` 3종. AI 컨테이너가 측정 → 백엔드 적재 |
| **평가지표** | LISTEN(listenText/listenPicture)·NAMING·SHADOWING·SELF_TALK (STORYTELLING 제외) — 턴 타입 = 채점 지표. 결과 점수는 `TURN.score`에 저장. LISTEN_TEXT/LISTEN_PICTURE는 운영상 구분일 뿐 지표상 동일 LISTEN |
| **AQ** | Aphasia Quotient (WAB 실어증 검사에서 유래). 세션 총점, 100점 만점 정수 |

---

## 2. 세션 구조

| 항목 | 확정 내용 |
|------|-----------|
| 세션 종류 (v1.1) | **2종 분기** — ① 오늘의 학습: 테마 서버 랜덤 선택, 8턴 순서·내용 모두 무작위 ② 테마별 학습: 유저가 테마 선택, **기획 시나리오 플로우** 그대로 진행 (문제 순서·내용·AI 대화 첫 턴 맥락까지 설계됨) |
| 턴 구성 | 평가지표 4종 × 각 2회 = **8턴** + STORYTELLING **4~8턴**. v1.3: LISTEN 2턴은 **LISTEN_TEXT 1회 + LISTEN_PICTURE 1회** (논의 기준 고정 — 실제 배분은 컨테이너 로직, 백엔드는 수신 type 기준 INSERT). v1.6: STORYTELLING **최소 완료 판정 = 유저 4턴째 답변** — 4턴 전 중단=학습 중단(상세 보고서 없음), 4턴 이후 중단=학습 완료 |
| 8턴 생성 | 세션 입장 시 서버가 **한 번에 전부 선생성** (AI 컨테이너 일괄 생성 1회 호출 — today/theme 엔드포인트 분기). 병렬화는 Redis+Celery 도입 후 |
| 이야기 턴 | 선생성 불가 — 진행 중 **한 턴씩 생성** |
| 총 턴 수 | 12~16턴 (8 + 4~8) |

---

## 3. 세션 진행 흐름

```
[오늘의 학습 입장]
  → 테마 랜덤 선택 → 세션 생성 (LEARNING_SESSION INSERT)
  → AI 컨테이너 POST /sessions (8문제 일괄 생성)
  → TURN 8행 INSERT (로컬 turnId 리매핑) + AI TTS OCI 적재
  → [로딩 화면 대기]

[문제 가이드 화면] (턴마다 — 안내 텍스트만, 안내 TTS 없음)
  → "준비됐어요!" 클릭 → <문제 화면>

[문제풀이 8턴] 순차 진행 — 제출 제한 30초 통일
  → 대기 카운트다운: LISTEN·SHADOWING 3초 후 TTS 재생 / NAMING·SELF_TALK 5초 (사진 관찰) — 냅다 재생 방지
  → SHADOWING: TTS 재생 종료 → 3초 후 녹음 시작 → 30초 제한
  → 음성형(NAMING·SHADOWING·SELF_TALK): [녹음 완료] 클릭 or 30초 도달 → 강제 제출 (클라가 30초에 녹음 컷 → multipart 전송)
  → LISTEN: 30초 도달 시 — 선택지 누름=마지막 선택지로 제출 / 미선택=오답 처리 제출. [다시 듣기]는 카운트다운 진행 중에도 사용 가능(무관)
  → SHADOWING: [다시 듣기] 없음 — 녹음 중 마이크 오염 방지
  → NAMING: 힌트 요청(최대 2회) 가능 — 30초 카운트다운은 그대로 진행 (시간 정지 없음)
  → 각 턴: 답안 제출 시마다 채점 요청 (제출자는 결과 대기 없이 다음 턴 진행)
  → LISTEN(listenText/listenPicture): 즉시 채점(백엔드 자체) / NAMING·SHADOWING·SELF_TALK: 컨테이너 동기 채점
  → "제출"(제한 있음)과 "다음으로"(제한 없음) 버튼 분리 — 다음으로 → 다음 문제 가이드 화면 (이동 자유)

[이야기 턴 4~8턴] ([학습 중단하기] 버튼 — 4턴째 답변 후 [학습 마치기]로 전환)
  → 첫 턴: 8턴 결과 + 유저 정보를 맥락으로 AI가 먼저 대화 개시
  → 이후: context 배열 기반 대화 진행
  → 8턴 하드캡: 유저가 8턴째 대화를 마쳤을 때 AI의 마지막 응답(9번째 응답)으로 마무리

[세션 종료] (정상 마무리 8턴 하드캡 / 학습 완료 4~7턴 / 학습 중단 1~3턴)

[학습 중단 판정 — 이야기 턴 1~3턴에서 중단]
  → /report/total 미호출 (상세 보고서 생성 안 함 — 중단 페널티)
  → 간이 보고서는 DB 저장됨(지표 활용)하나 유저에게 표시하지 않음 + 학습 기록탭에도 미표시
  → 클라: 우는 덕분이 팝업 확인 → 나가기

[학습 완료 판정 — 4턴째 답변 이후 중단/마치기]
  → /report/total 호출 (유저 4턴째 답변까지만 포함 — 이후 AI 생성 중이어도 유저 답변까지만)
  → 간이 보고서 화면 이동 + 상세 보고서 백그라운드 생성

[8턴 하드캡]
  → 유저 8턴째 답변 후 AI 마무리 응답(9번째) → [학습 결과 보기] → 간이 보고서 + 상세 백그라운드
  → AI 컨테이너 POST /report → sessionAQ + 피드백 6종 수신
  → 결과 화면 로딩 대기 후 리포트 표시
  → 공유폴더 음성 파일 정리
```

### 채점 결과 수신 방식 (확정)

- **클라 ↔ BE**: 논블로킹 제출 — 유저는 채점 결과를 기다리지 않고 계속 진행
- **BE ↔ 컨테이너**: 동기 REST (`POST /answer/*`) — 응답받아 TURN.score / VOICE_RECORD 적재
- 이야기 턴이 채점 처리시간을 벌어주는 구조 → 이야기 턴 동안 대부분의 채점 완료
- 턴당 채점 결과는 **세션 결과 화면에서 일괄 표시** (턴당 피드백은 없음)
- ⚠️ pending: 이 부분 상세 수신 방식은 팀장 상의 후 재통보 예정

---

## 3. 타입별 상호작용 매트릭스

| 타입 | AI 음성 | 유저 입력 | 유저 음성 | 발화지표 | 채점 | 이미지 |
|------|---------|-----------|-----------|----------|------|--------|
| **LISTEN_TEXT** (알아듣기-텍스트) | O (TTS 질문) | **화면 선택지 탭** (발화 아님) | X | X | 백엔드 자체 (정답 100/오답 0) | 텍스트 선택지 2~4개 (등급별) |
| **LISTEN_PICTURE** (알아듣기-그림) | O (TTS 질문) | **화면 선택지 탭** (발화 아님) | X | X | 백엔드 자체 (정답 100/오답 0) | 이미지 선택지 2~4개 (등급별 — imageListListening 내) |
| **NAMING** (이름대기) | O | 발화 (STT) | O | O | 컨테이너 (절대정답) | 이미지 1개 (**EASY 전용** — 사물) |
| **SHADOWING** (따라말하기) | O | 발화 (따라말하기) | O | O | 컨테이너 (원문 대비) | X |
| **SELF_TALK** (스스로말하기) | O | 발화 (상황 묘사) | O | O | 컨테이너 (태그 비교) | 상황 이미지 1개 |
| **STORYTELLING** (이야기하기) | **X** (지연 우려, 향후 추가 가능) | 음성 (STT만) | O (기록만) | **X** | **X** | X |

### 타입별 상세

**LISTEN_TEXT** — AI가 TTS로 문제를 읽어주면 화면의 **텍스트 선택지**(등급별 2~4개)를 **탭으로 선택**. 예: "오늘은 금요일인가요?". `correct_value`/`selected_value` = 선택지 ref. 유저 음성·VOICE_RECORD 행 없음. 출제 재료: 개인화 정보(userInfos) **활용 가능** — 테마 맞춤도 가능 (컨테이너 자율). 3초 카운트다운 후 TTS 재생 → 30초 제출 제한, [다시 듣기] 제공(카운트다운 진행 중에도 무관). 30초 도달: 선택 누름=최근 선택지 제출 / 미선택=오답 처리.

**LISTEN_PICTURE** — 동일 구조·동일 시간 규약, **이미지 선택지**만 (imageListListening 배열 내 — TURN_IMAGE에 order로). 출제 재료: DB 이미지 리스트만.

**LISTEN 난이도 등급표 (v1.3)** — userAQ 5등급으로 EASY/HARD 태그와 선택지 개수 결정. 백엔드 = 태그 필터(imageListListening), 컨테이너 = 선택지 개수. 상세 등급표: 03 계약서 §2.1.

**NAMING** — 이미지 1개 보고 발화로 답 (예: "마우스"). 절대정답 존재. **5초 카운트다운(사진 관찰) 후 녹음 시작 → 30초 제출 제한** — [녹음 완료] or 30초 도달 시 제출. 힌트는 **유저가 요청 버튼을 눌렀을 때** 공개 — 항상 **의미단서 → 조음단서 고정 순서**, 1개씩, 공개될 때마다 감점 (감점 수식은 컨테이너 내부 루브릭 — 백엔드 비관여). **힌트 요청은 30초 카운트다운과 무관(시간 정지 없음)**. 힌트 2종(SEMANTIC_CUE/ARTICULATORY_CUE)은 **반드시 각 1개씩 존재**. `hints_shown`에 노출 수 기록 (0 허용).

**SHADOWING** — AI TTS 문장을 따라 말함. 이미지 미사용. 정답 Text를 백엔드가 보유 (`problemContext`로 컨테이너 전달). **3초 카운트다운 후 TTS 재생 → 재생 종료 후 3초 후 녹음 시작 → 30초 제출 제한**. **[다시 듣기] 없음** — 녹음 중 TTS가 마이크에 들어가 오염 방지 (v1.6).

**SELF_TALK** — 단일 사물이 아닌 "여러 일이 벌어지는 상황" 이미지. 유저가 상황을 묘사하면 **tags.json 내용과 유저 발화(STT)를 비교**해 채점. tags.json은 백엔드가 OCI에서 읽어 **내용 그대로** 컨테이너에 전달 (파싱 불필요 — 규약대로 업로드됨). `correct_value` = NULL. **5초 카운트다운(사진 관찰) 후 녹음 시작 → 30초 제출 제한** — 30초 다 채울 필요 없음.

**STORYTELLING (v1.6 UX 규약)** — 상황극 자유대화. 채점 없음. 첫 턴은 8턴 결과 + 유저 정보 기반으로 AI가 먼저 첫 대사. 유저 발화는 STT → `TURN.answer_text`, AI 발화 → `TURN.prompt_text`. AI 음성/발화지표 없음 (DB 구조는 확장 대비 유지).

- 우상단 **[학습 중단하기]** 버튼: 1~3턴 동안 표시 — 클릭 시 우는 덕분이 팝업("덕분이와 n번만 더 대화해주세요. 잠깐만! 지금 나가면 학습 결과를 확인할 수 없어요!") → [네, 나갈게요]=학습 중단 판정 / [아니요, 계속할게요]=계속
- 유저 4턴째 답변 완료 시 버튼이 **[학습 마치기]**로 전환 — 클릭 시 "수고하셨어요! 이번 학습은 여기까지 할까요?" 팝업 → 나가면 학습 완료 판정 (간이 보고서 이동 + 상세 백그라운드)
- 8턴째 답변 후 AI 마무리 응답(9번째) → "이번 학습 수고하셨어요!" [학습 결과 보기]
- 유저 답변: [음성으로 답변하기] 버튼 직접 클릭으로 시작 (제한시간 없음 — 녹음 30초 제한은 표시). 녹음 중 확실한 UI(알파 패널+마이크 대문짝). [녹음 완료] or 30초 도달 → 자동 제출 → 덕분이 턴
- LLM 로딩 중: "덕분이가 답변을 생각중이에요" 표시

---

## 4. 힌트 (NAMING)

| 항목 | 확정 내용 |
|------|-----------|
| 저장 | OCI hint.json 폐지 → **DB 컬럼 직접 저장** (`IMAGE_RESOURCE.SEMANTIC_CUE` / `ARTICULATORY_CUE`, nullable) |
| 공개 방식 | 유저가 힌트 요청 버튼 클릭 → 백엔드가 의미단서→조음단서 순으로 1개 반환 (동기) + `HINTS_SHOWN` 증가 |
| 출제 대상 (v1.3) | **EASY 이미지 전용** — 사물 이미지. HARD(행동·상황·사람)은 NAMING 불가 |
| 존재 보장 | 2종 반드시 각 1개씩 (NAMING용 이미지는 cue 컬럼 필수 데이터) |
| 감점 | 공개마다 감점 — 수식은 컨테이너 루브릭 (계약서 `hintCount`로 전달) |

## 5. 리포트 (세션 결과) — 2단계 분리 (v1.1)

> **UX 개선 (2026-09-04 합의):** 보고서 생성 대기 시간이 길어 유저가 멍하니 기다리는 문제 → 2단계 분리.

| 항목 | 확정 내용 |
|------|-----------|
| **1단계: 간이 보고서** | 8번째 문제 제출 시점에 백엔드가 자동으로 컨테이너 `POST /report/problems` 호출 — AQ(8문제만 산출) + 4지표 피드백 생성. **이야기 턴이 진행되는 동안 완성** → 세션 종료 시 즉시 표시 (멍때리기 제거) |
| **2단계: 상세 보고서** | 세션 종료(대화 종료·**학습 완료 판정**) 시점에 `POST /report/total` — talkFeedback+totalFeedback 생성. **학습 중단(1~3턴)은 미호출**. 백그라운드 처리, 완료 후 **앱 내 알림함·네비 버블**로 알림 |
| 알림 UX | 상세 보고서 생성 완료 → 알림 수령 → 조회 시 `LEARNING_SESSION.REPORT_VIEWED_AT` 기록 (null=미조회 — 알림 판별) |
| 저장 위치 | REPORT 테이블 신설 안 함 — `LEARNING_SESSION` 통합 (AQ+피드백 6컬럼). 적재는 2단계로 분리: 4지표 피드백+AQ=problems 시점 / talk+total 피드백=total 시점 |
| 표시 내용 | 간이: AQ + 4지표 피드백 + 턴별 점수. 상세(알림 이후): + 대화 피드백 + 종합 피드백 |
| AQ | 100점 만점 정수 — **8문제 점수만으로 산출** (AI 대화 미포함, 2026-09-04 확정). 소수점 올림은 컨테이너 책임 |
| 학습 중단 (이야기 1~3턴) | STATUS=`COMPLETED_NO_TALK` — /report/total **미호출** (상세 보고서 생성 안 함 — 중단 페널티). 간이 보고서는 DB 저장(지표 활용)하나 **유저 표시 안 함 + 학습 기록탭 미표시** |
| 학습 완료 (4턴째 답변 이후) | /report/total **호출** — 유저 4턴째 답변까지만 포함 (AI 생성 중이어도 유저 답변까지만). 판정 기준은 깔끔히 "유저가 4턴째 답변을 입력했는가"만 |
| 스텁 지연 | problems 2~3초 / total 10초 |
| 알림 기능 | 클라 신규 태스크 — **후순위** (최종 발표 후 시간 남으면. 데모 제외) |

## 5.1 개인화 데이터 (userInfos — v1.2)

| 항목 | 확정 내용 |
|------|-----------|
| 프로필 (가입 플로우) | 닉네임 + 프로필 사진 + 성별 + **생년월일**(BIRTH_DATE) + 취미(자유입력, HOBBIES) + **관심사 태그**(TAGS 마스터 15종 중 최대 5개 선택) |
| userInfos 전달 | `{nickname, hobbies, tags, sex, age, userMemory}` — /sessions/today·theme, /aichat에 주입해 출제·대화 개인화. age는 BIRTH_DATE 기반 백엔드 산정 |
| **userMemory (에이전트 메모리)** | AI 자유대화에서 얻은 개인 맥락을 누적 관리해 다음 세션 개인화에 활용. **갱신 = /report/total 1곳만** (기존값+talkContext → 갱신값 반환). 실패·소득 없음 시 기존 값 유지. 저장 = USER_PROFILE.USER_MEMORY(CLOB, 오파크 — 백엔드 파싱 비관여, 8KB 하드캡). 탈퇴 시 자동 파기. 규약: 03a §10 |
| 민감정보 | memory 저장 금지 항목(의료·질병 등)은 프롬프트 레벨 통제 — 발표 시 "민감정보 저장 방지 노력" 언급 |

## 5.2 가입 설문조사 — 초기 AQ 확보 (v1.4)

> **목적:** 첫 사용자는 AQ(학습 이력)가 없어 userAQ 산출 불가 → LISTEN 등급 산정·개인화가 공백. 가입 시 설문으로 초기 AQ를 만든다 (03 계약서 §2.1 등급표의 "userAQ null 발생 없음" 약속의 실제 구현).

| 항목 | 확정 내용 |
|------|-----------|
| 등장 조건 | 신규 가입자 — 회원가입 페이지(프로필사진·닉네임·생년월일 등) **직후**. **스킵 불가** |
| 재노출 규칙 | **USER_AQ = null** 유저는 다음 로그인 시 설문 재노출 1회 (가입 직후 앱 종료 방어). 설문 강제 플로우상 USER_AQ 비null = 설문 응답 보증 |
| 문항 구성 | 5문항 × 1~5 라디오 버튼 (1=전혀 못해요 ~ 5=아주 잘해요) — 문항은 앱에 고정:
  1. 일상에서 하고 싶은 말을 원하는 만큼 표현하시나요?
  2. 상대방의 말을 끝까지 이해하는 편이신가요?
  3. 물건이나 사람의 이름이 잘 떠오르시나요?
  4. 들은 문장을 그대로 따라 말하기가 편하신가요?
  5. 대화를 이어가는데 부담이 적으신가요? |
| 점수 산출 | 응답 × 4 = 문항 점수 (각 20점 만점) → 5문항 총점 20~100 |
| AQ 환산 | **20~61 → AQ 30 / 62~80 → AQ 70 / 81~100 → AQ 90** |
| 저장 | **USER_REPRESENTATIVE_SCORES.USER_AQ** (NUMBER(3) nullable — 04 v2.6 §4.2.2, 대표점수 전용 테이블) — 설문 환산값으로 **초기 세팅**. **환산 AQ만 저장 — 응답 원문은 저장하지 않음** |
| 산출 주체 | **서버** (설문 접수 API — 클라 계산값 아님. 수식 단일화·조작 방지) |
| userAQ 연동 | **USER_AQ 단일 캐시 (v1.5)** — 설문 접수 시 USER_AQ 초기 세팅 → 이후 AQ 확정 시점(/report/problems 수신)마다 재계산 UPDATE. /sessions 요청은 USER_PROFILE에서 바로 조회 (계산 분기 없음, 추가 쿼리 0). **null = 설문 미응답** → 설문 재노출 판별 기준 |
| isNewUser 판별 | 기존 로그인 버그 있음 — **후속 수정 예정** (기능 개발 완료 후). 설문 재노출 규칙은 **USER_AQ null** 기준으로 isNewUser와 독립 동작 |

## 6. 실력 산정 (대시보드)

| 항목 | 확정 내용 |
|------|-----------|
| 산정식 | **최근 20개 세션 중 지표별 상위 10개 세션의 평균** |
| userAQ | 동일식 (AQ 기준) — **USER_REPRESENTATIVE_SCORES.USER_AQ 캐시에서 직접 조회 전달** (산정식은 /report/problems 수신 시 1회 실행 — 갱신은 write-path). null 없음 (v1.5: 가입 설문으로 항상 확보) |
| 등급 활용 (v1.3) | 세션 생성 시 USER_REPRESENTATIVE_SCORES.USER_AQ 조회 → **LISTEN 난이도 등급 산정** (03 계약서 §2.1) → imageListListening EASY/HARD 태그 필터. 매 세션 생성 (최신 캐시값 기준 — 캐시는 /report/problems 수신 시마다 갱신) |

## 7. 상태 관리 / 이어하기

| 항목 | 확정 내용 |
|------|-----------|
| `LEARNING_SESSION.status` | IN_PROGRESS / COMPLETED / (폐기 등) |
| `TURN.status` | **PENDING**(출제·미풀이) / **SUBMITTED**(답안 제출) / **SCORED**(채점 완료) |
| 이어하기 | 세션 재입장 시 TURN.status로 진행 위치 판별 → 이어하기 제공 |
| 폐기·신규 | 미완료 세션 폐기 + 신규 생성 옵션도 제공 |
| 공유폴더 정리 | 세션 종료/폐기 시점에 삭제 |

## 7.1 대시보드 / 학습 기록 (v1.7 실구현 확정)

> 대시보드 탭: 유저 대표 지표(방사형) + 지난 학습 결과 카드 리스트 — 기존 스텁을 실제 DB 값으로 교체. 상세 API 규약: `05a_Client_API_Reference.md` §8.

| 항목 | 확정 내용 |
|------|-----------|
| 방사형 그래프 (대시보드) | **USER_REPRESENTATIVE_SCORES** 대표점수 5종(AQ+4지표) — v2.6 신설 전용 테이블 (USER_PROFILE.USER_AQ 안이 이동 통합된 최종안) |
| 지난 학습 카드 | 세션명(`SESSION_NAME` — today=`오늘의 학습 - {테마명}` / theme=`병원에서 진료받기`·`동네카페에서` 등 시나리오명) + 날짜(createdAt ISO 수신 → 클라가 YYYY.mm.dd 포맷) + `AQ nn점` |
| 리스트 조회 조건 | **STATUS != COMPLETED_NO_TALK** — 학습 중단 세션 미표시 (간이 보고서는 DB에 있으나 카드 리스트에서 제외) |
| 세부 보고서 방사형 | **해당 세션 TURN.score 집계** (대표점수 아님 — 출처 구분) |
| 세부 보고서 구성 | 종합 피드백(TOTAL_FEEDBACK) + AQ + 지표 방사형(TURN 집계) + 지표별 카드(점수·피드백, 클릭 확장 → 문제 기록) + AI 대화 피드백/내역 |
| 문제 기록 표시 | 지표 카드 확장 시 — 문제 안내·AI TTS(재생)·내 답변(선택지 텍스트/그림, 음성) 유형별 구성 |
| AI 대화 내역 | AI text + 내 답변 **다시 듣기** (음성 재생) |
| 조회 시점 기록 | 세부 보고서 조회 시 `REPORT_VIEWED_AT` 갱신 (null=미조회 — 알림 판별 규약과 연동) |

## 8. 세션 생성 시퀀스 (8턴 INSERT 전략)

```
POST /sessions/today · /theme (BE → 컨테이너): 테마/이미지리스트 3분할(listening=등급 태그 필터 적용)/유저정보(userInfos=userMemory 포함)/userAQ 전달
  ← problemList 8개 (turnId는 컨테이너 로컬 번호 1~8)
백엔드: TURN 8행 INSERT (시퀀스로 실제 PK 발급 — UPDATE 불필요)
  · 로컬 turnId 순서 → turn_number
  · type → content_type / passage → prompt_text
  · listenText·listenPicture correct/options → correct_value / choices_json
  · naming correct → correct_value
TTS: 공유폴더 {로컬턴ID}_ai.mp3 → 실제 TURN PK로 리네임 → OCI 적재
```

> 근거 (ADR-006): 턴 선 INSERT 시 어느 턴이 어떤 타입/문제를 가질지 알 수 없어 UPDATE가 2배 발생. 응답 수신 시점에 리매핑하면 INSERT 1회로 해결. 컨테이너는 자기가 만든 문제를 기억하지 않음.

## 9. pending (미확정)

| 항목 | 상태 |
|------|------|
| 턴당 채점 결과 수신 방식 상세 | 팀장 상의 후 재통보 |
| 리포트 생성 실패 재시도 | 고도화 과제 |
| BE↔컨테이너 타임아웃/에러 규약 | 고도화 과제 (데모 단계 규약 없음) |
| STORYTELLING AI 음성(TTS) 추가 | 향후 확장 — DB 구조는 유지 |
| userAQ 가입 설문 주입 | ✅ **확정 (v1.4 §5.2 + v1.5)** — 가입 설문으로 USER_AQ 초기 세팅 (구현은 백엔드+클라 후속 태스크) |
| 상세 보고서 알림함·네비 버블 (클라) | 신규 태스크 — **후순위** (최종 발표 후 여유 시) |
| 테마별 학습 시나리오 콘텐츠 | 시나리오 **이름은 확정** (예: "병원에서 진료받기", "동네카페에서" — SESSION_NAME 저장) — 문제 상세 시퀀스·AI 첫 턴 맥락 등 플로우 데이터는 컨텐츠 팀 확정 대기 |

## 10. 변경 이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v1.0 | 2026-09-02 | 초안 확정 — 기획 회의 전체 확정사항 반영 |
| v1.2 | 2026-09-04 | **컨테이너 협의 반영 (2) — userMemory 개인화:** §5.1 신설 — 가입 플로우 개편(생년월일·취미·태그 15종 중 5개), userInfos 개편(userMemory 포함), userMemory 라이프사이클(갱신=/report/total 1곳, 활용=/sessions·/aichat, 저장=CLOB, 파기=탈퇴 연동). TAGS/USER_PROFILE_TAGS 정규화(04 v2.2) |




| v1.7 | 2026-09-04 | **컨테이너 협의 확정 (7) — 대시보드/기록 실구현:** §7.1 신설 — (1) 대시보드 방사형=USER_REPRESENTATIVE_SCORES 대표점수(AQ+4지표, LISTEN 통합) (2) 지난 학습 카드=SESSION_NAME(시나리오명 확정: "병원에서 진료받기"·"동네카페에서")·YYYY.mm.dd·AQ nn점 (3) 리스트 조회 STATUS != COMPLETED_NO_TALK (4) 세부 보고서 방사형=해당 세션 TURN.score 집계, 구성요소 확정(지표 카드 확장→문제 기록, AI 대화 내역+다시 듣기), REPORT_VIEWED_AT 갱신 연동 |
| v1.1 | 2026-09-04 | **컨테이너 협의 반영 (1):** §2 세션 2종 분기(오늘의 학습=무작위 / 테마별 학습=기획 시나리오 플로우). §5 리포트 2단계 분리 — 간이 보고서(/report/problems, 8턴 종료 시 AQ+4지표, 이야기 중 백그라운드 완성) / 상세 보고서(/report/total, 종료 시 백그라운드+앱 내 알림, REPORT_VIEWED_AT 조회 추적), AQ=8문제만 산출 확정, 조기종료 COMPLETED_NO_TALK. 알림 기능은 후순위 |